#!/usr/bin/env node
/**
 * RedScript LSP server entry point.
 * Communicates over stdio (standard LSP protocol).
 *
 * Usage: redscript-lsp  (started by your editor's LSP client)
 */

import './server'
